r.11  -- fixed - fixed folder downloading. for some reason, mega will have
                 multiple handles and their respective keys in its .k key.
                 the script now searches for the correct key and uses it.
                 this fixes #1.
r.10  -- added - replaced #!/bin/bash with #!/usr/bin/env bash for nixos users
                 and for more portability across distros.
               - aria2 logging for better error investigation.
      !!       - added new folder link key parsing.
                 started at or around 2026-04-04, .k now has three values.
                 i do not know what this new value is.
               - added pipefail + error check for folder key decryption (see above)
         fixed - missing filename output on file link download completion
               - variable typo in size mismatch message
rev.9 -- fixed - removed carriage return (\r) from windows text files
               - file in folder path notice, would warn that path was set regardless
rev.8 -- added part 1/2 of more link sanity checking
         fixed - subfolder downloading (only a singular file instead of all)
               - some error messages didn't output colour properly
               - api error code checking/detection
         clarified the subfolder downloading part in README
         changed some wording in some messages
rev.7 -- added check for missing commands
         cleaned up README
rev.6 -- replaced worker url to prepare for more widespread use
         partial downloads will now truncate if size mismatches (decryption interrupted)
         tweaked existing file function
rev.5 -- added proper retry logic for downloading for files/folders
         removed file id from output, replacee with filename
         made output more verbose and consistent
         tweaked worker script to give 4xx on /, this fixes empty downloads
rev.4 -- moved file downloading logic for folders into function
         made folder downloading faster by downloading in parallel (&)
         disabled download/decryption progress because of above change
         added very rudimentary error handling for downloading
         made output more verbose
         added another edge case handling for partial downloads
         lowered aria2's max connection from 3 to 2
         cleaned up some variables
         added more comments
rev.3 -- added '--compressed' arg to curl, some folders need it
rev.2 -- fixed subfolder path detection
         forgot to add pv to list of requirements
         added size variable in decrypt functions so pv will give progress
rev.1 -- initial private release
